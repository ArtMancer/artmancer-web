# Endpoints are imported individually to avoid loading unnecessary dependencies
# Heavy Service: from app.api.endpoints import generation
# Light Service: from app.api.endpoints import smart_mask, image_utils
